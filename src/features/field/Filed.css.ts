import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
    field: {
        cursor: 'pointer',
        display: 'inline-flex',
        border: '1px solid'
    },
    marked: {
        backgroundColor: 'red'
    }
}));
